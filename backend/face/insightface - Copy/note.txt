install these before activate it:
- pip -m py -r requirements.txt
- .\venv\Scripts\Activate.ps1
- pip install insightface
- pip install insightface --only-binary :all:

Activate the virtual environment
1. .\venv\Scripts\Activate.ps1
2. python face_compare.py

Facial recognition
    Passive score - 80%
        Movement score - 50%
        Texture - 30%
        if passive score >= 70%, pass
        if passive score < 70%, proceed to active challenge

    Active challenge
        Capture movement - 3 seconds, detect liveness
        if liveness = pass, check similarity score(Passive score)
        if similarity score > 60%, pass
        if similarity score <= 60%, fail